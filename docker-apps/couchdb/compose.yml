services:
  couchdb:
    image: couchdb:3.5
    container_name: couchdb
    ports:
      - "5984:5984"
    environment:
      - COUCHDB_USER=admin
      - COUCHDB_PASSWORD=admin_password
    volumes:
      - ${DATASET_ROOT:-.}/couchdb/data:/opt/couchdb/data
